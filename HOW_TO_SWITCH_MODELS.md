# 🤖 模型切换 - 快速使用指南

## 📍 在哪里找到模型选择器？

### ✅ 全局导航栏（推荐）
模型选择器现在显示在**应用的顶部导航栏**，在所有页面都能看到！

```
┌─────────────────────────────────────────────────────────┐
│  ← 首页        🤖 AI 模型: [DeepSeek v]    │
│                                                          │
│         📱 应用内容在这里                                 │
└─────────────────────────────────────────────────────────┘
```

## 🎯 如何切换模型？

1. **打开应用** 
   - 访问 http://localhost:3000
   
2. **找到导航栏**
   - 顶部可以看到 "🤖 AI 模型:" 的下拉框
   
3. **点击下拉框**
   - 显示所有可用的模型：
     - ✅ DeepSeek (默认)
     - ✅ Xiaomi MiMo (新增)
     - ✅ OpenAI (如果配置了)

4. **选择模型**
   - 点击要使用的模型名称
   - 选择会自动保存到本地存储

## 📝 选择会如何影响应用？

一旦你选择了一个模型：

- ✅ **自动应用** - 所有后续的 API 调用都会使用这个模型
- ✅ **持久化** - 刷新页面后选择仍然保留
- ✅ **全局有效** - 所有功能页面都使用你的选择
  - 随机推荐
  - 占卜
  - AI吐槽
  - 自制奶茶
  - 等等...

## 🔄 工作流示例

```
1. 用户打开应用
    ↓
2. 在导航栏看到"🤖 AI 模型: DeepSeek (默认)"
    ↓
3. 点击下拉框，选择 "Xiaomi MiMo"
    ↓
4. 选择保存到 localStorage
    ↓
5. 点击"随机奶茶"按钮
    ↓
6. 应用自动使用 MiMo 模型调用 API
    ↓
7. 显示结果
```

## 🎨 UI 位置说明

### 首页 (HomePage)
```
────────────────────────────────────
│  ← 首页  │  🤖 AI 模型: [选择]  │
────────────────────────────────────
│                                    │
│   🎲 随机奶茶                       │
│   🧪 自制奶茶                       │
│   🎮 游戏中心                       │
│                                    │
────────────────────────────────────
```

### 功能页面 (如随机页面)
```
────────────────────────────────────
│  ← 首页  │  🤖 AI 模型: [选择]  │
────────────────────────────────────
│                                    │
│   🎲 命运盲盒                       │
│   [转轮动画]                        │
│   [确认按钮]                        │
│                                    │
────────────────────────────────────
```

## 💡 提示

### 🎯 首次使用
如果看不到模型选择器，可能是：
- ❌ 后端没有启动 → 运行 `npm start` 在 server 目录
- ❌ 没有配置API Key → 检查 .env 文件中的 MIMO_API_KEY
- ❌ 只有一个模型可用 → 配置多个模型的 API Key

### ⚡ 快速测试
1. 保证后端在 localhost:3000 运行
2. 打开浏览器访问应用
3. 查看导航栏的模型选择器
4. 尝试切换不同模型
5. 检查响应是否来自不同的 AI

## 🔧 技术细节

### 存储位置
```javascript
// 选择的模型保存在
localStorage.getItem('selectedModel')

// 示例值
'deepseek' | 'mimo' | 'openai'
```

### API 调用
```javascript
// 模型选择会自动传递给后端
POST /api/prompt/random?model=mimo
{
  "userId": "...",
  "contextTags": [...]
}
```

## 📞 故障排除

| 问题 | 解决方案 |
|------|--------|
| 看不到模型选择器 | 检查浏览器控制台是否有错误，重新加载页面 |
| 模型列表为空 | 确保至少有一个模型的 API Key 在 .env 中 |
| 选择不起作用 | 清除浏览器 localStorage: `localStorage.clear()` |
| 切换后没有效果 | 检查网络标签页，确保请求发送了 ?model=xxx 参数 |

---

## 🎉 就这么简单！

现在你可以轻松在不同的 AI 模型之间切换了！

**选择你首选的模型，享受不同的回答风格吧！** 🚀
